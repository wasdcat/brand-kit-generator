## What it does

Brand Kit Generator produces transparent PNG overlays for social media, streams and video. What an overlay looks like is decided by its template — the tool supplies brand, colours, fonts and text.

It does not edit video and does not render it — it delivers the graphic. It runs in the browser, with nothing to install.

## Key Features

* **Transparent PNG output, or a finished picture:** On its own an export is a PNG with a transparent background, meant to sit as the topmost layer over video or stills in any editor or streaming software. Put a background image underneath and that image is exported with the overlay — the file is then ready to upload.
* **Five independent building blocks:** Product, template, colour set, font set and content are chosen separately. A different colour set changes the colour without touching the layout, a different template changes the layout without retyping the text.
* **Four output formats:** The same template renders at 1:1, 9:16, 16:9 and 4:5, and adapts its layout to each one.
* **Bundled fonts and colour sets:** Seven open-source variable font families ship with the tool, alongside colour sets of seven colour roles each.
* **Runs offline:** No accounts, no tracking, no build step, no network requests — everything happens in the browser. All four formats can be exported at once as a ZIP archive.

## Templates

Four of them ship with the tool: a full-width lower third, plus three examples — a corner bug, an angled bar and a full-frame title card — that show three different ways of using the same mechanics.

A template is an HTML file whose entire design sits in its CSS. Anyone who knows HTML and CSS can build one without touching program code, and every template file can be opened on its own in a browser — it then shows sample values and looks just as it does in the application.

## Your setup stays local

Everything you set up is kept in your browser and can be saved to a file — as a backup, to move it onto another machine, or to keep several setups side by side, one per client.

## How to use

You can run the web version directly via [GitHub Pages](https://wasdcat.github.io/brand-kit-generator/) or download the source code from [GitHub](https://github.com/wasdcat/brand-kit-generator).

## License and credits

* **Author:** Frank Winter / WASDCAT Games
* **License:** [MIT](https://github.com/wasdcat/brand-kit-generator/blob/main/LICENSE) — free for personal and commercial use
* **Fonts:** SIL Open Font License 1.1
* **Source code:** [GitHub](https://github.com/wasdcat/brand-kit-generator)
* **Manual:** [English](https://github.com/wasdcat/brand-kit-generator/blob/main/docs/manual/en/README.md) · [German](https://github.com/wasdcat/brand-kit-generator/blob/main/docs/manual/de/README.md)
